version: '3'
services:
  # Database #
  db:
    image: mysql:8.0.33-debian
    restart: always
    cap_add:
      - SYS_NICE
    networks:
      - internal_network
    environment:
      - MYSQL_DATABASE=porcalabs
      - MYSQL_USER=user
      - MYSQL_PASSWORD=Test_!23
      - MYSQL_ROOT_PASSWORD=RootAA!23
    volumes:
      - db:/var/lib/mysql
  # Phpmyadmin #
  phpmyadmin:
    depends_on:
      - db
    image: phpmyadmin:5.2.1-fpm-alpine
    restart: always
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: RootAA!23
      PMA_ABSOLUTE_URI: https://serv.porcalabs.com/phpmyadmin/
    networks:
      - internal_network
  # API #
  api:
    build: ./api
    image: api
    restart: always
    networks:
      - internal_network
    volumes:
      - api:/main
  # WordPress #
  wordpress:
    depends_on:
      - db
    image: wordpress:6.2.2-fpm-alpine
    restart: always
    volumes: ['./wordpress/:/var/www/html']
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: user
      WORDPRESS_DB_PASSWORD: RootAA!23
    networks:
      - internal_network
  # web:
  #   build: ./web
  #   image: web
  #   restart: always
  #   networks:
  #     - internal_network
  #   depends_on:
  #     - db
  #   volumes:
  #     - web:/var/www/
  # Dozzle #
  dozzle:
    image: amir20/dozzle:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - internal_network
    environment:
      DOZZLE_NO_ANALYTICS: "true"
      DOZZLE_BASE: /logs
      DOZZLE_ADDR: "0.0.0.0:1212"
  # Nginx #
  nginx:
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    networks:
      - internal_network
      - external_network
    volumes:
      - ./nginx/conf/:/etc/nginx/conf.d/:ro
      - web:/var/www/
      - ./wordpress/:/var/www/html
      - ./certbot/www:/var/www/certbot/:ro
      - ./certbot/conf/:/etc/nginx/ssl/:ro
    command: ["nginx", "-g", "daemon off;"]
  # Certbot #
  certbot:
    image: certbot/certbot:latest
    volumes:
      - ./certbot/www/:/var/www/certbot/:rw
      - ./certbot/conf/:/etc/letsencrypt/:rw

networks:
  internal_network:
  external_network:

volumes:
  api:
  web:
  db:
    driver: local
